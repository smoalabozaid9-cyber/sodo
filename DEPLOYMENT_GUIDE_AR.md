# دليل النشر الشامل - نظام إدارة مكتب المحاماة

هذا الدليل يغطي خيارات متعددة لنشر تطبيقك على الإنترنت.

## 📋 جدول المحتويات

1. [التحضير قبل النشر](#preparation)
2. [الخيار 1: Heroku (الأسهل - متوفر مجاناً)](#heroku)
3. [الخيار 2: DigitalOcean (موصى به - احترافي)](#digitalocean)
4. [الخيار 3: AWS (مستوى المؤسسات)](#aws)
5. [استضافة قاعدة البيانات (MongoDB Atlas)](#database)
6. [إعداد النطاق وشهادة SSL](#domain-ssl)
7. [قائمة المراجعة بعد النشر](#checklist)

---

## 🎯 التحضير قبل النشر {#preparation}

### الخطوة 1: تجهيز التطبيق للإنتاج

#### 1.1 تحديث متغيرات البيئة

أنشئ ملف `.env` للإنتاج بقيم آمنة:

```env
# متغيرات بيئة الإنتاج
NODE_ENV=production
PORT=5000

# اتصال MongoDB Atlas (سنقوم بإعداده)
MONGODB_URI=رابط_اتصال_mongodb_atlas

# مهم جداً: أنشئ مفتاح JWT قوي
JWT_SECRET=مفتاح_عشوائي_طويل_جداً_32_حرف_على_الأقل

# اختياري: إعدادات البريد الإلكتروني
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=كلمة_مرور_التطبيق
```

#### 1.2 إنشاء مفتاح JWT آمن

استخدم إحدى هذه الطرق:

**الطريقة 1: مولد على الإنترنت**
- اذهب إلى: https://randomkeygen.com/
- استخدم قسم "CodeIgniter Encryption Keys"
- انسخ المفتاح

**الطريقة 2: أمر Node.js**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**الطريقة 3: أمر PowerShell**
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

#### 1.3 بناء الواجهة الأمامية

```bash
cd client
npm run build
cd ..
```

هذا ينشئ نسخة إنتاج محسّنة في `client/build/`.

---

## 🚀 الخيار 1: النشر على Heroku (الأسهل) {#heroku}

**الأفضل لـ**: النشر السريع، طبقة مجانية متوفرة، أسهل إعداد

### الخطوة 1: إنشاء حساب Heroku

1. اذهب إلى: https://www.heroku.com/
2. انقر على "Sign Up"
3. أنشئ حساب مجاني

### الخطوة 2: تثبيت Heroku CLI

**Windows:**
حمّل من: https://devcenter.heroku.com/articles/heroku-cli

### الخطوة 3: تجهيز التطبيق

1. **تسجيل الدخول إلى Heroku**
```bash
heroku login
```

2. **تهيئة Git (إذا لم يكن موجوداً)**
```bash
git init
git add .
git commit -m "Initial commit for deployment"
```

3. **إنشاء تطبيق Heroku**
```bash
# إنشاء التطبيق
heroku create

# أو إنشاء باسم مخصص
heroku create your-lawyer-office-app
```

### الخطوة 4: إعداد MongoDB Atlas (قاعدة البيانات)

انظر [قسم استضافة قاعدة البيانات](#database) أدناه.

### الخطوة 5: تكوين متغيرات البيئة

```bash
heroku config:set NODE_ENV=production
heroku config:set JWT_SECRET="مفتاحك_الآمن_هنا"
heroku config:set MONGODB_URI="رابط_اتصال_mongodb_atlas"
```

### الخطوة 6: إنشاء Procfile

أنشئ ملفاً باسم `Procfile` (بدون امتداد) في المجلد الرئيسي:

```
web: node server.js
```

### الخطوة 7: النشر!

```bash
# النشر على Heroku
git add .
git commit -m "Prepare for Heroku deployment"
git push heroku main

# إذا كنت تستخدم فرع master:
git push heroku master
```

### الخطوة 8: فتح التطبيق

```bash
heroku open
```

### الخطوة 9: عرض السجلات

```bash
heroku logs --tail
```

### أوامر Heroku المهمة

```bash
# عرض متغيرات التكوين
heroku config

# تعيين متغير تكوين
heroku config:set KEY=VALUE

# إعادة تشغيل التطبيق
heroku restart

# عرض السجلات
heroku logs --tail
```

---

## 🌊 الخيار 2: النشر على DigitalOcean {#digitalocean}

**الأفضل لـ**: النشر الاحترافي، تحكم كامل، سعر معقول ($5-10/شهر)

### الخطوة 1: إنشاء حساب DigitalOcean

1. اذهب إلى: https://www.digitalocean.com/
2. سجّل حساباً
3. احصل على رصيد $200 (إذا كان متاحاً) للمستخدمين الجدد

### الخطوة 2: إنشاء Droplet (خادم)

1. انقر "Create" → "Droplets"
2. اختر:
   - **Image**: Ubuntu 22.04 LTS
   - **Plan**: Basic ($6/شهر كحد أدنى)
   - **CPU**: Regular with SSD
   - **Datacenter**: اختر الأقرب لمستخدميك
   - **Authentication**: SSH Key (موصى به) أو Password
3. انقر "Create Droplet"

### الخطوة 3: الاتصال بالخادم

```bash
ssh root@عنوان_ip_الخادم
```

**مستخدمو Windows يمكنهم استخدام:**
- PuTTY: https://www.putty.org/
- أو Windows PowerShell/Terminal

### الخطوة 4: إعداد الخادم

```bash
# تحديث النظام
apt update && apt upgrade -y

# تثبيت Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# تثبيت Git
apt install -y git

# تثبيت PM2 (مدير العمليات)
npm install -g pm2

# تثبيت Nginx (خادم الويب)
apt install -y nginx
```

### الخطوة 5: نشر التطبيق

```bash
# رفع الملفات باستخدام SCP/SFTP
# من جهازك المحلي:
scp -r /مسار/المشروع root@عنوان_ip:/var/www/lawyer-office

# أو استنساخ من Git
cd /var/www
git clone رابط_المستودع lawyer-office
cd lawyer-office

# تثبيت التبعيات
npm install
cd client && npm install && npm run build && cd ..

# إنشاء ملف .env
nano .env
# الصق متغيرات البيئة الإنتاجية
# اضغط Ctrl+X، ثم Y، ثم Enter للحفظ
```

### الخطوة 6: بدء التطبيق باستخدام PM2

```bash
# بدء التطبيق
pm2 start server.js --name lawyer-office

# تشغيل PM2 عند بدء التشغيل
pm2 startup
pm2 save

# عرض السجلات
pm2 logs lawyer-office

# أوامر PM2 الأخرى
pm2 status                    # التحقق من الحالة
pm2 restart lawyer-office     # إعادة التشغيل
pm2 stop lawyer-office        # إيقاف التطبيق
```

### الخطوة 7: تكوين Nginx

```bash
# إنشاء تكوين Nginx
nano /etc/nginx/sites-available/lawyer-office
```

الصق هذا التكوين:

```nginx
server {
    listen 80;
    server_name your_domain.com www.your_domain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# تفعيل الموقع
ln -s /etc/nginx/sites-available/lawyer-office /etc/nginx/sites-enabled/

# اختبار تكوين Nginx
nginx -t

# إعادة تشغيل Nginx
systemctl restart nginx

# تمكين Nginx عند بدء التشغيل
systemctl enable nginx
```

### الخطوة 8: إعداد الجدار الناري

```bash
# السماح بـ SSH و HTTP و HTTPS
ufw allow OpenSSH
ufw allow 'Nginx Full'
ufw enable
ufw status
```

### الخطوة 9: تثبيت شهادة SSL (HTTPS)

```bash
# تثبيت Certbot
apt install -y certbot python3-certbot-nginx

# الحصول على شهادة SSL
certbot --nginx -d your_domain.com -d www.your_domain.com

# اتبع التعليمات
# ستُجدد الشهادة تلقائياً
```

---

## 🗄️ استضافة قاعدة البيانات (MongoDB Atlas) {#database}

**موصى به لجميع خيارات النشر**

### الخطوة 1: إنشاء حساب MongoDB Atlas

1. اذهب إلى: https://www.mongodb.com/cloud/atlas
2. سجّل حساباً مجانياً
3. أنشئ مجموعة جديدة (Cluster) - متوفر مجاناً

### الخطوة 2: إعداد المجموعة

1. اختر:
   - **Cloud Provider**: AWS أو Google Cloud أو Azure
   - **Region**: اختر الأقرب لمستخدميك
   - **Cluster Tier**: M0 Sandbox (مجاني)
2. انقر "Create Cluster"
3. انتظر 3-5 دقائق

### الخطوة 3: إنشاء مستخدم قاعدة البيانات

1. اذهب إلى "Database Access"
2. انقر "Add New Database User"
3. اختر طريقة المصادقة: Password
4. أنشئ اسم مستخدم وكلمة مرور
5. عيّن الامتيازات: "Read and write to any database"
6. انقر "Add User"

### الخطوة 4: السماح بعناوين IP

1. اذهب إلى "Network Access"
2. انقر "Add IP Address"
3. الخيارات:
   - **للتطوير**: انقر "Allow Access from Anywhere" (0.0.0.0/0)
   - **للإنتاج**: أضف عنوان IP الخادم الخاص بك
4. انقر "Confirm"

### الخطوة 5: الحصول على رابط الاتصال

1. اذهب إلى "Clusters"
2. انقر "Connect" على مجموعتك
3. اختر "Connect your application"
4. انسخ رابط الاتصال
5. استبدل `<password>` بكلمة مرور مستخدم قاعدة البيانات
6. استبدل `<dbname>` باسم قاعدة البيانات (مثلاً: `lawyer-office`)

**مثال على رابط الاتصال:**
```
mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/lawyer-office?retryWrites=true&w=majority
```

### الخطوة 6: تحديث التطبيق

أضف رابط الاتصال إلى `.env` أو منصة النشر:

```env
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/lawyer-office?retryWrites=true&w=majority
```

---

## 🌐 إعداد النطاق وشهادة SSL {#domain-ssl}

### الخطوة 1: تسجيل نطاق

**مسجلو النطاقات الشائعون:**
- Namecheap: https://www.namecheap.com/
- GoDaddy: https://www.godaddy.com/
- Google Domains: https://domains.google/
- Cloudflare: https://www.cloudflare.com/products/registrar/

**أسماء نطاقات موصى بها:**
- yourfirm-legal.com
- yourfirmlawoffice.com
- [اسمك]law.com

### الخطوة 2: توجيه النطاق إلى الخادم

**لـ Heroku:**
1. اذهب إلى Heroku Dashboard
2. Settings → Domains
3. أضف نطاقك المخصص
4. حدّث سجلات DNS عند مسجل النطاق:
   - النوع: CNAME
   - الاسم: www
   - القيمة: your-app.herokuapp.com

**لـ DigitalOcean/VPS:**
1. احصل على عنوان IP الخادم
2. اذهب إلى إعدادات DNS لمسجل النطاق
3. أضف سجل A:
   - النوع: A
   - الاسم: @
   - القيمة: عنوان_ip_الخادم
4. أضف سجل A لـ www:
   - النوع: A
   - الاسم: www
   - القيمة: عنوان_ip_الخادم

### الخطوة 3: شهادة SSL (HTTPS)

**لـ Heroku:**
- SSL تلقائي مع النطاقات المخصصة (الخطط المدفوعة)

**لـ DigitalOcean/VPS:**
- استخدم Let's Encrypt (مجاني) مع Certbot (كما هو موضح أعلاه)

**لـ Cloudflare (موصى به - مجاني):**
1. سجّل في https://www.cloudflare.com/
2. أضف نطاقك
3. غيّر خوادم الأسماء عند المسجل إلى Cloudflare
4. فعّل SSL/TLS (Flexible أو Full)
5. HTTPS تلقائي

---

## ✅ قائمة المراجعة بعد النشر {#checklist}

### الأمان

- [ ] تم تغيير JWT_SECRET إلى قيمة عشوائية قوية
- [ ] كلمة مرور MongoDB قوية وفريدة
- [ ] تم تكوين الجدار الناري (UFW/Security Groups)
- [ ] تم تثبيت شهادة SSL (HTTPS)
- [ ] تم تعيين متغيرات البيئة بشكل صحيح
- [ ] تم تقييد وصول شبكة MongoDB
- [ ] تم تحديث الخادم
- [ ] PM2 أو مدير العمليات يعمل
- [ ] تم تكوين النسخ الاحتياطي

### الأداء

- [ ] تم تحسين بناء الواجهة الأمامية
- [ ] تم إنشاء فهارس MongoDB
- [ ] تم تمكين التخزين المؤقت لـ Nginx
- [ ] تم تكوين CDN (اختياري - Cloudflare)
- [ ] تحسين الصور
- [ ] تم تمكين ضغط Gzip

### المراقبة

- [ ] إعداد تسجيل الأخطاء
- [ ] تكوين مراقبة وقت التشغيل (UptimeRobot)
- [ ] إعداد مراقبة PM2
- [ ] مراقبة قاعدة البيانات (MongoDB Atlas)
- [ ] إعداد تنبيهات التوقف

### الاختبار

- [ ] تسجيل حساب مستخدم تجريبي
- [ ] إنشاء عميل تجريبي
- [ ] إنشاء قضية تجريبية
- [ ] اختبار جميع عمليات CRUD
- [ ] الاختبار على الأجهزة المحمولة
- [ ] التحقق من تحميل جميع الصفحات بشكل صحيح

---

## 💰 مقارنة التكاليف

| الخيار | التكلفة الشهرية | صعوبة الإعداد | قابلية التوسع |
|--------|-----------------|---------------|--------------|
| **Heroku مجاني** | $0 | سهل جداً | محدود |
| **Heroku Hobby** | $7 | سهل جداً | جيد |
| **DigitalOcean** | $6-12 | متوسط | ممتاز |
| **AWS EC2** | $5-20 | متوسط | ممتاز |
| **VPS (Contabo)** | $5-10 | متوسط | جيد |

**MongoDB Atlas**: طبقة مجانية متوفرة (512MB)

---

## 🔧 المشاكل الشائعة والحلول

### المشكلة: التطبيق لا يبدأ

**الحل:**
```bash
# التحقق من السجلات
pm2 logs

# التحقق مما إذا كان المنفذ متاحاً
netstat -tulpn | grep :5000

# إعادة تشغيل التطبيق
pm2 restart lawyer-office
```

### المشكلة: لا يمكن الاتصال بـ MongoDB

**الحل:**
- تحقق من القائمة البيضاء لـ MongoDB Atlas
- تحقق من صحة رابط الاتصال
- تحقق من اسم المستخدم/كلمة المرور
- تأكد من وجود اسم قاعدة البيانات

### المشكلة: 502 Bad Gateway (Nginx)

**الحل:**
```bash
# تحقق مما إذا كان التطبيق يعمل
pm2 status

# تحقق من سجلات Nginx
tail -f /var/log/nginx/error.log

# إعادة تشغيل Nginx
systemctl restart nginx
```

### المشكلة: النطاق لا يشير إلى الخادم

**الحل:**
- انتظر 24-48 ساعة لنشر DNS
- تحقق من سجلات DNS: `nslookup your_domain.com`
- تحقق من أن سجلات A تشير إلى IP الصحيح

---

## 📞 الخطوات التالية

1. **اختر منصة النشر** (Heroku موصى به للمبتدئين)
2. **أعد MongoDB Atlas** (مطلوب لجميع الخيارات)
3. **اتبع الدليل المحدد** للمنصة التي اخترتها
4. **اختبر بدقة** قبل الإطلاق
5. **أعد المراقبة** والنسخ الاحتياطي

---

**هل أنت مستعد للنشر؟ ابدأ بـ MongoDB Atlas، ثم اختر منصة الاستضافة!** 🚀

هل تحتاج مساعدة في نشر محدد؟ أخبرني أي منصة تفضل!
