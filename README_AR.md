# نظام إدارة مكتب المحاماة - نظام إدارة القضايا

تطبيق SaaS شامل لمكاتب المحاماة لإدارة القضايا والعملاء والبيانات القانونية بكفاءة.

## 🎯 المميزات

- **نظام المصادقة**: نظام آمن لتسجيل الدخول والتسجيل مع صلاحيات حسب الدور (مدير، محامي، مساعد قانوني)
- **إدارة القضايا**: إنشاء وتحديث وتتبع وإدارة القضايا القانونية مع معلومات تفصيلية
- **إدارة العملاء**: تخزين وإدارة معلومات العملاء وتفاصيل الاتصال والمستندات
- **لوحة التحكم**: نظرة عامة على القضايا والإحصائيات والإجراءات السريعة
- **ملاحظات القضايا**: إضافة ملاحظات وتتبع تقدم القضية
- **تصفية متقدمة**: تصفية القضايا حسب الحالة والنوع والأولوية
- **تصميم متجاوب**: يعمل على أجهزة الكمبيوتر المكتبي والأجهزة اللوحية والجوال

## 🛠️ التقنيات المستخدمة

### الواجهة الخلفية (Backend)
- **Node.js** - بيئة التشغيل
- **Express.js** - إطار عمل الويب
- **MongoDB** - قاعدة البيانات
- **Mongoose** - مكتبة ODM لـ MongoDB
- **JWT** - المصادقة
- **bcryptjs** - تشفير كلمات المرور

### الواجهة الأمامية (Frontend)
- **React** - مكتبة واجهة المستخدم
- **React Router** - التنقل
- **Axios** - عميل HTTP
- **CSS3** - التنسيق

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت ما يلي:
- Node.js (الإصدار 14 أو أحدث)
- MongoDB (الإصدار 4.4 أو أحدث)
- npm أو yarn

## 🚀 التثبيت

### 1. تحميل المشروع

```bash
# استخراج ملف ZIP والانتقال إلى المجلد
cd lawyer-office-saas
```

### 2. تثبيت التبعيات للواجهة الخلفية

```bash
npm install
```

### 3. تثبيت التبعيات للواجهة الأمامية

```bash
cd client
npm install
cd ..
```

### 4. إعداد متغيرات البيئة

أنشئ ملف `.env` في المجلد الرئيسي:

```bash
cp .env.example .env
```

قم بتحرير ملف `.env` بإعداداتك:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/lawyer-office
JWT_SECRET=مفتاح_سري_آمن_قم_بتغييره_في_الإنتاج
NODE_ENV=development
```

**مهم**: قم بتغيير `JWT_SECRET` إلى نص عشوائي آمن في الإنتاج!

### 5. تشغيل MongoDB

تأكد من تشغيل MongoDB على نظامك:

```bash
# على Windows (إذا تم تثبيته كخدمة)
# يجب أن يبدأ MongoDB تلقائيًا

# للتحقق من تشغيل الخدمة
net start MongoDB
```

### 6. تشغيل التطبيق

**الخيار أ: تشغيل الواجهة الخلفية والأمامية بشكل منفصل (موصى به للتطوير)**

نافذة الأوامر 1 - تشغيل الواجهة الخلفية:
```bash
npm run dev
```

نافذة الأوامر 2 - تشغيل الواجهة الأمامية:
```bash
npm run client
```

**الخيار ب: استخدام ملفات .bat المجهزة (الأسهل)**

ببساطة انقر نقرًا مزدوجًا على:
```
START_HERE.bat
```

### 7. الوصول إلى التطبيق

افتح متصفحك وانتقل إلى:
- الواجهة الأمامية: `http://localhost:3000` (التطوير)
- API الواجهة الخلفية: `http://localhost:5000`

## 👤 إعداد المستخدم الأول

بما أن هذا تثبيت جديد، ستحتاج إلى تسجيل المستخدم الأول:

1. اذهب إلى `http://localhost:3000/register`
2. املأ نموذج التسجيل
3. اختر الدور: مدير النظام (للوصول الكامل)
4. انقر على "تسجيل"

## 📱 هيكل التطبيق

```
lawyer-office-saas/
├── client/                 # واجهة React الأمامية
│   ├── public/            # الملفات الثابتة
│   └── src/
│       ├── components/    # مكونات React
│       ├── context/       # سياق المصادقة
│       ├── App.js         # المكون الرئيسي
│       └── index.js       # نقطة الدخول
├── models/                # نماذج قاعدة البيانات
│   ├── User.js           # نموذج المستخدم
│   ├── Client.js         # نموذج العميل
│   └── Case.js           # نموذج القضية
├── routes/               # مسارات API
│   ├── auth.js          # مسارات المصادقة
│   ├── clients.js       # مسارات العملاء
│   └── cases.js         # مسارات القضايا
├── middleware/          # وسيط Express
│   └── auth.js         # وسيط المصادقة
├── server.js           # خادم Express
└── package.json        # التبعيات
```

## 🎨 واجهة المستخدم

### لوحة التحكم
- عرض إحصائيات القضايا
- قائمة القضايا الأخيرة
- أزرار الإجراءات السريعة

### القضايا
- عرض جميع القضايا مع التصفية
- إنشاء قضايا جديدة
- عرض تفاصيل القضية
- إضافة ملاحظات للقضايا
- تتبع تقدم القضية

### العملاء
- عرض جميع العملاء
- وظيفة البحث
- إنشاء عملاء جدد
- عرض ملفات العملاء
- رؤية قضايا العميل

## 🔐 ميزات الأمان

- تشفير كلمات المرور باستخدام bcrypt
- المصادقة المستندة إلى JWT
- مسارات API محمية
- التحكم في الوصول بناءً على الدور
- تكوين متغيرات البيئة

## 🧪 اختبار التطبيق

1. **تسجيل مستخدم**: أنشئ حساب المسؤول الأول الخاص بك
2. **إنشاء عميل**: أضف عميلًا جديدًا مع معلومات الاتصال
3. **إنشاء قضية**: اربط قضية بالعميل الذي أنشأته
4. **إضافة ملاحظات**: أضف ملاحظات القضية وتتبع التقدم
5. **اختبار التصفية**: استخدم المرشحات للعثور على قضايا محددة
6. **تحديث الحالة**: غيّر حالة القضية والأولوية

## 📊 أنواع القضايا المدعومة

- جنائية
- مدنية
- أحوال شخصية
- تجارية
- عقارية
- هجرة
- ملكية فكرية
- عمالية
- ضريبية
- أخرى

## 📈 حالات القضايا

- مفتوحة
- قيد التنفيذ
- معلقة
- مغلقة
- مكسوبة
- خاسرة
- تسوية

## 🎯 مستويات الأولوية

- منخفضة
- متوسطة
- عالية
- عاجلة

## 🐛 استكشاف الأخطاء وإصلاحها

### خطأ اتصال MongoDB
- تأكد من تشغيل MongoDB
- تحقق من `MONGODB_URI` في ملف `.env`
- تحقق من إمكانية الوصول إلى MongoDB عبر URI المحدد

### المنفذ قيد الاستخدام بالفعل
- قم بتغيير PORT في ملف `.env`
- أو أوقف العملية التي تستخدم المنفذ

### لا يمكن تسجيل الدخول
- تحقق من وجود المستخدم في قاعدة البيانات
- تحقق من تعيين JWT_SECRET بشكل صحيح
- امسح ذاكرة التخزين المؤقت وملفات تعريف الارتباط للمتصفح

### الواجهة الأمامية لا تحمّل
- تأكد من تشغيل الواجهة الخلفية
- تحقق من إعداد الوكيل في `client/package.json`
- قم بتشغيل `npm install` في مجلد client

## 🚀 النشر للإنتاج

### التحضير
1. عيّن `NODE_ENV=production` في `.env`
2. استخدم `JWT_SECRET` قوي
3. استخدم قاعدة بيانات MongoDB إنتاجية (يوصى بـ MongoDB Atlas)
4. ابنِ الواجهة الأمامية: `npm run build`

## 📝 التحسينات المستقبلية

- [ ] تحميل المستندات وإدارتها
- [ ] تكامل التقويم لمواعيد المحكمة
- [ ] إشعارات البريد الإلكتروني
- [ ] الفواتير والتسعير
- [ ] تتبع الوقت
- [ ] تقارير متقدمة
- [ ] بوابة العملاء
- [ ] دعم متعدد اللغات

## 📄 الترخيص

ترخيص MIT - لا تتردد في استخدام هذا المشروع لمكتب المحاماة الخاص بك أو تعديله حسب الحاجة.

## 👨‍💻 الدعم

للمشاكل أو الأسئلة أو المساهمات:
- أنشئ مشكلة في المستودع
- اتصل بفريق التطوير الخاص بك

## 🙏 شكر وتقدير

تم بناؤه بتقنيات الويب الحديثة لمساعدة مكاتب المحاماة على إدارة قضاياها بكفاءة.

---

**إدارة قضايا سعيدة! ⚖️**
